## Commit Message Convention

Format: `{skill-name}: {description}`

- The prefix before the colon is the skill's directory name, using hyphens (e.g., `session-handoff`, `skill-creator`, `implementation-planning`)
- For changes spanning multiple skills or repo-wide changes, use a general prefix (e.g., `sync`, `chore`)
